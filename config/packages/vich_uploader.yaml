vich_uploader:
    db_driver: orm

    mappings:
        participant_image:
            uri_prefix: /image/participant
            upload_destination: '%kernel.project_dir%/public/image/participant'
            namer: Vich\UploaderBundle\Naming\UniqidNamer

    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
