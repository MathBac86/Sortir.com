{% extends 'base2.html.twig' %}

{% block title %}Profile | Modif{% endblock %}

{% block main %}
    <div class="Modif">
        <h1>Modification</h1>
        {{ form_start(Registerform) }}
        {{ form_widget(Registerform) }}
        <button type="submit" class="btn btn-success mb-2">Modifier</button>
        {{ form_end(Registerform) }}
        <a href="{{ path('participant') }}" class="btn btn-primary mb-2" >Retour profile</a>
    </div>
{% endblock %}